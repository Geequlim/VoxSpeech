version: 1

engine:
  backend: auto
  idleTimeout: 10m
  maxBatch: 1

model:
  default: qwen3-tts-1.7b-base-q4_k_m

voice:
  default: assistant

api:
  enabled: true
  host: 127.0.0.1
  port: 8080

audio:
  format: wav
